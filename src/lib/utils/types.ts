export type Todo = {
        id: string;
        title: string;
        description: string;
        dueDate: string;
        priority: string;
        completed: boolean;
};

export type Profile = {
        image: string;
        firstName: string;
        lastName: string;
        email: string;
}